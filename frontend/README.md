# ACTA Frontend

React + Vite frontend prepared for local development and static hosting on GitHub Pages.

## Environment

```bash
VITE_API_URL=http://localhost:3000
```

## Local run

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The build output is generated in `dist/` with `index.html` and `assets/`.
